cargo-features = ["panic-immediate-abort"]

[package]
name = "bnkterm"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "immediate-abort"
strip = true
