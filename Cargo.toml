cargo-features = ["panic-immediate-abort"]

[package]
name = "bnkterm"
version = "0.2.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "immediate-abort"
strip = true
